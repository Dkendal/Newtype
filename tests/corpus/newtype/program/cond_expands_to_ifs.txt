A cond inside a type alias expands to a chain of ifs

=======

type A as cond do
    a <: b -> c,
    d <: e -> f,
    else -> g
end

=======

type A as
    if a <: b then c
    else
        if d <: e then f
        else g
        end
    end
