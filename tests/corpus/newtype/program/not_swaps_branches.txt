A `not` condition in a type alias swaps the if branches

=======

type A as
    if not (a <: b) then
        c
    else
        d
    end

=======

type A as
    if a <: b then
        d
    else
        c
    end
