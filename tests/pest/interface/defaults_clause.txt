With defaults clause

=======

interface Foo(A) defaults A = any {}

=======

(program
  (statement
    (interface
      (ident: "Foo")
      (type_parameters
        (ident: "A")
      )
      (defaults_caluse
        (type_parameter_default
          (ident: "A")
          (expr
            (any: "any")
          )
        )
      )
      (object_literal: "{}")
    )
  )
)
